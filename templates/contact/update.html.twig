{% extends 'base.html.twig' %}

{% set title = "Edition de #{ contact.lastname }, #{ contact.firstname }" %}
{% block title %}{{ title }}{% endblock %}

{% block stylesheets %}
{%  endblock %}

{% block body %}
    <h1>{{ title }}</h1>
    {{ form_start(form, {'method' :'POST'}) }}
    {{ form_widget(form) }}
    <input class="btn btn-primary w-100" type="button" value="Modifier">
    {{ form_end(form) }}
{% endblock %}



